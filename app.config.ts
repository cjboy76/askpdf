export default defineAppConfig({
  ui: {
    primary: 'teal',
    gray: 'cool',
    notifications: {
      position: 'top-0 bottom-auto',
    },
    notification: {
      default: {
        timeout: 3000
      }
    }
  }
})

